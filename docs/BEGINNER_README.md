# PCEP Roadmap

---

## 📘 1. Basic Concepts (17%)

### Computing & Programming Basics
- What is a computer program?
- Interpreters vs. compilers
- Python language characteristics
- How Python executes code
- Python keywords & identifiers

### Python Execution Model
- How to run Python scripts
- Using the interactive shell
- Script vs. module execution

### Basic Syntax & Structure
- Indentation rules
- Comments (single and multi-line)
- Naming conventions
- Error types: `SyntaxError`, `RuntimeError`

---

## 📗 2. Data Types, Variables, Basic I/O (20%)

### Built-in Data Types
- Integers
- Floats
- Strings
- Booleans

### Variables & Assignment
- Simple assignment
- Multiple assignment
- Type casting: `int()`, `float()`, `str()`
- Using `input()` & `print()`

### Operators
- Arithmetic: `+`, `-`, `*`, `/`, `//`, `%`, `**`
- Comparison: `==`, `!=`, `>`, `<`, `>=`, `<=`
- Logical: `and`, `or`, `not`
- String: concatenation & repetition

---

## 📙 3. Control Flow – Conditions & Loops (26%)

### Conditional Statements
- `if`
- `if-else`
- `if-elif-else`
- Nested conditions

### Looping Statements
- `while` loop
- `for` loop
- Loop control: `break`, `continue`, `pass`

### Range & Iteration
- Using `range()`
- Iterating through strings, lists

---

## 📕 4. Data Collections – Lists, Tuples, Strings, Dictionaries (23%)

### Strings
- Indexing
- Slicing
- Basic methods: `.upper()`, `.lower()`, `.find()`, `.replace()`, `.split()`

### Lists
- Indexing, slicing
- Append, insert, remove, pop
- List methods: `.sort()`, `.reverse()`, `.count()`, `.index()`

### Tuples
- Tuple creation
- Tuple immutability
- Tuple unpacking

### Dictionaries
- Key-value pairs
- Access, insert, update, delete
- Methods: `.keys()`, `.values()`, `.items()`

---

## 📔 5. Functions (14%)

### Function Basics
- Defining functions with `def`
- Parameters & return values
- Default arguments
- Positional vs. keyword arguments

### Scope
- Local vs. global variables
- The `global` keyword

### Built-in Functions
- `len()`, `type()`, `abs()`, `round()`, `min()`, `max()`, etc.
